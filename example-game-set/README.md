# Example game-set

Copy this folder into `data/game_sets/example` to make it available in the host and editor. It contains one deliberately simple round and two tiny SVG images so the folder remains suitable for Git.

